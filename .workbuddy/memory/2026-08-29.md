## 2026-08-29

- 为 BarePingwidget（裸连监测）项目撰写开源文档：
  - 新增 `README.md`（中文）与 `README_EN.md`（英文），两文件顶部徽章互链跳转。
  - 升级根 `.gitignore` 为完整 Android 忽略清单（忽略构建产物、local.properties、.idea 等，保留 doc/ 截图）。
  - 新增根 `LICENSE`（MIT）。
  - 关键说明：用户需求中的「自动翻页/阅读进度/定时关闭/自动签到」等描述与实际代码不符，实际为裸连监测 App；README 按真实代码独立撰写中英两份。
  - README 中 stars 徽章与仓库链接使用占位 `USERNAME/BarePingWidget`，上传前需替换用户名；截图需放入 `doc/1.jpg`、`doc/2.jpg`、`doc/3.jpg`。
